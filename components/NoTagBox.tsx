import { Flex } from "@chakra-ui/react";
import React from "react";

const NoTagBox = () => {
  return (
    <Flex justify="center" textAlign="center" py="8" mt="10" opacity="0.4">
      Este producto no contiene ninguna etiqueta
    </Flex>
  );
};

export default NoTagBox;
